<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Msgtrik" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/purple_web</item>
    </style>

    <style name="Theme.Msgtrik" parent="Base.Theme.Msgtrik" />

    <style name="PurpleDatePicker" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">@color/purple_web</item>
        <item name="android:colorAccent">@color/purple_web</item>
        <item name="colorPrimary">@color/purple_web</item>
        <item name="android:colorPrimary">@color/purple_web</item>
        <item name="android:buttonBarPositiveButtonStyle">@style/PurpleButton</item>
        <item name="android:buttonBarNegativeButtonStyle">@style/PurpleButton</item>
    </style>

    <style name="PurpleButton" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="android:textColor">@color/purple_web</item>
    </style>
</resources>